defaults:
  - global_conf: default
  - data_conf: default
  - model_conf: default
  - run_conf: default
  - _self_

model_conf:
  conf:
    layer:
      layer_n: 4
      layer_size: [ 300, 300, 300, 300 ]

run_conf:
  train_conf:
    epoch: 30